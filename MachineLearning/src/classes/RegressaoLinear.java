package classes;
/*
 * y = a* bx - Essa é a linha
 * 
 * Gradiente Descent = 1/(2m) * E(ho(x) - y)^2
 * E = Soma
 * ho = o teta, onde
 * ho(x) = a *bx (predição) = ho(x)
 * 
 * Definir quem é teta 0 teta 1, quem é o a e quem é o b
 */

public class RegressaoLinear {

	private double t0;
	private double t1;
	
	
}
