package testes;

import java.util.*;
import java.io.*;

public class TiposJava {
    public static void main(String []argh)
    {

    	byte  a = 5;
		short b = -150;
		int   c = 150000;
		//    	1500000000
		//    	213333333333333333333333333333333333
		//    	-100000000000000
		
		double dValor = 0.0;
            try
            {
            	//if(x>=-128 && x<=127)System.out.printf("* byte   [%d] - [%d]", Math.pow(2.0, 7), (-1)*(Math.pow(2.0, 7)));   //7
            	System.out.printf("* byte   [%f] - [%f]\n", Math.pow(2.0,   7.0), (-1)*(Math.pow(2.0,   7.0)));   //7
            	System.out.printf("* short  [%f] - [%f]\n", Math.pow(2.0,  15.0), (-1)*(Math.pow(2.0,  15.0)));   //15
            	System.out.printf("* int    [%f] - [%f]\n", Math.pow(2.0,  31.0), (-1)*(Math.pow(2.0,  31.0)));   //31
            	System.out.printf("* long   [%f] - [%f]\n", Math.pow(2.0,  63.0), (-1)*(Math.pow(2.0,  63.0)));   //63
            	System.out.printf("* float  [%f] - [%f]\n", Math.pow(2.0, 127.0), (-1)*(Math.pow(2.0, 127.0)));   //127
            	System.out.printf("* double [%f] - [%f]\n", Math.pow(2.0, 265.0), (-1)*(Math.pow(2.0, 265.0)));   //265
 
            	
            	//long x=sc.nextLong();
                //System.out.println(x+" can be fitted in:");
                //if(x>=-128 && x<=127)System.out.println("* byte");
                //Complete the code
            }
            catch(Exception e)
            {
                //System.out.println(sc.next()+" can't be fitted anywhere.");
            	System.out.println(" can't be fitted anywhere.");
            }
    }
}