package testes;

import java.util.*;
import java.io.*;

public class TiposJava {
    public static void main(String []argh)
    {

byte  a = 5;
short b = -150;
int   c = 150000;
//    	1500000000
//    	213333333333333333333333333333333333
//    	-100000000000000

double dValor = 0.0;
            try
            {
            	//if(x>=-128 && x<=127)System.out.printf("* byte   [%d] - [%d]", Math.pow(2.0, 7), (-1)*(Math.pow(2.0, 7)));   //7
            	System.out.printf("* byte   [%d] - [%d]", Math.pow(2.0,   7), (-1)*(Math.pow(2.0,   7)));   //7
            	System.out.printf("* short  [%d] - [%d]", Math.pow(2.0,  15), (-1)*(Math.pow(2.0,  15)));   //15
            	System.out.printf("* int    [%d] - [%d]", Math.pow(2.0,  31), (-1)*(Math.pow(2.0,  31)));   //31
            	System.out.printf("* long   [%d] - [%d]", Math.pow(2.0,  63), (-1)*(Math.pow(2.0,  63)));   //63
            	System.out.printf("* float  [%d] - [%d]", Math.pow(2.0, 127), (-1)*(Math.pow(2.0, 127)));   //127
            	System.out.printf("* double [%d] - [%d]", Math.pow(2.0, 265), (-1)*(Math.pow(2.0, 265)));   //265
 
            	
            	//long x=sc.nextLong();
                //System.out.println(x+" can be fitted in:");
                //if(x>=-128 && x<=127)System.out.println("* byte");
                //Complete the code
            }
            catch(Exception e)
            {
                System.out.println(sc.next()+" can't be fitted anywhere.");
            }

}